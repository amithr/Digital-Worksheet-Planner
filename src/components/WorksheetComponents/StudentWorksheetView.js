import React, { useEffect, useState } from 'react';
import { observer } from "mobx-react";
import {Button} from 'react-bootstrap';

const StudentWorksheetView = observer((props => {

    return(
        <div>

        </div>
    )
}));

export default StudentWorksheetView;